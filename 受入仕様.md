# 受入仕様 ── ベンダーに渡す1枚

各システムのベンダーに「毎日ここへ、この名前で置いてください」と依頼するための仕様。
**交渉ごとはこの1枚に集約する。** ここが決まれば、あとは機械が取り込む。

この文書と `instance/config.json` の `sources` は同じことを言っている。
**片方だけ直さない** ── 文書に書いただけの規約は守られないので、
規約は `config.json` を正とし、この文書はそれを人が読む形にしたもの。

---

## 1. 置き場

社内ファイルサーバの共有フォルダ1つ。ベンダーはここに置くだけ。

```
\\<ファイルサーバ>\城\incoming\
```

- 書き込み権限は、各ベンダーのサービスアカウントのみ
- **取り込み済みのファイルは機械が `取込済\YYYY-MM\` へ移す。** ベンダーは消さない
- 取り込めなかったものは `保留\` に残る。ここに溜まったら人が見る

## 2. ファイル名

**名前が仕様である。** 名前が違うと取り込まれず、保留に退けられる。
（推測で種別を当てにいくと、間違った種別のまま記録に入るので、推測はしない設計）

| システム | ファイル名 | 中身 | 出す頻度 |
|---|---|---|---|
| A社 販売管理 | `A社販売管理_売上日報_YYYY.csv` | 部門別・日次の売上 | 毎営業日 |
| A社 販売管理 | `A社販売管理_仕入日報_YYYY.csv` | 部門別・日次の仕入 | 毎営業日 |
| A社 販売管理 | `A社販売管理_週次在庫_YYYY.csv` | 部門別・週次の実地棚卸 | 毎週（棚卸の翌営業日） |
| B社 勤怠 | `B社勤怠_勤務実績_YYYY.csv` | 組織別・日次の実労働時間 | 毎営業日 |
| C社 会計 | `C社会計_部門別損益_YYYYMM.csv` | 部門別の売上・原価・人件費・販管費 | 月次（締めの翌営業日） |
| C社 会計 | `C社会計_試算表_YYYYMM.csv` | 科目別の残高（営業外・特別・税） | 月次（同上） |
| C社 会計 | `C社会計_月末残高_YYYYMM.csv` | 科目別の月末残高 | 月次（同上） |

`YYYY` は年、`YYYYMM` は年月。同じ名前で送り直してもよい（**中身が変わっていれば取り込み直す**）。

## 3. 形式

| 項目 | 仕様 |
|---|---|
| 文字コード | Shift_JIS（CP932）／UTF-8 どちらでも可。**機械が判定する** |
| 改行 | CRLF／LF どちらでも可 |
| 区切り | カンマ |
| 見出し行 | 必須。列の順番は問わない（**見出しの名前で拾う**） |
| 単位 | 円 または 千円。**千円なら見出しに「(千円)」と書く** |
| 日付 | `YYYY/MM/DD` `YYYY-MM-DD` どちらでも可 |
| 月次表の年月 | 行に日付列が無い形でよい。**見出しの前の行に「〜年〜月度」と書く** |

**ここに書いていない揺れは、こちらで吸収する。** ベンダーの都合で列順が変わっても、
見出しの名前が同じなら取り込める。1000倍・全角半角・部門名の表記ゆれも吸収する。

## 4. 交渉で押さえる4点

これだけ合意できれば足りる。**逆に、これが決まらないと自動化は始まらない。**

1. **置き場に書き込めること** ── サービスアカウント1つ。人の手を介さない
2. **名前が固定であること** ── 日付やバージョンを名前に足さない（上の表のとおり）
3. **出す時刻が決まっていること** ── 何時までに置くか。見張りはその後に走る
4. **止まったときに連絡が来ること** ── 機械側でも遅れは検知するが、原因はベンダーしか知らない

**追加開発を求めない。** 既にどのシステムにもCSV出力はある。
「決まった名前で、決まった場所へ、決まった時刻に吐く」だけを頼む。

## 5. 届かなかったとき

**静かに古いデータで画面が出るのが、いちばん悪い。**
だから機械の側でも見張る。

| 周期 | 許す遅れ | 超えたら |
|---|---|---|
| 日次（売上・仕入・勤怠） | 4日 | 画面の上に赤で名指し |
| 週次（在庫） | 11日 | 同上 |
| 月次（会計3種） | 締め日＋5日 | 同上 |

見張りは、保留が出たり取り込めなかったりすると**終了コードを0以外で返す**。
タスクスケジューラの「前回の実行結果」で気づける。

## 6. 動かし方

```
python castle\app\intake.py              置き場を一巡して取り込み、画面まで作り直す
python castle\app\intake.py --dry-run    取り込まず、何をするつもりかだけ言う
python castle\app\intake.py --freshness  どのサイロがどこまで届いているかを見る
```

毎営業日の決まった時刻に、Windows タスクスケジューラから `見張り.bat` を1回呼ぶ。
（`見張り.bat` は python を直接呼ぶ1段構成。多段の起動チェーンにしない ──
常駐のウイルス対策が、多段のスクリプト起動を無言で止めることがあるため）
